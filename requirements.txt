fastapi>=0.100.0
uvicorn>=0.22.0
sqlalchemy>=2.0.0
pydantic>=2.0.0
zeep>=4.2.0,<5.0.0
cryptography>=42.0.0  # Cifrado simétrico AES (Fernet) para tokens
python-dateutil>=2.8.2
httpx>=0.24.0
jinja2>=3.1.2
python-dotenv>=1.0.0
requests>=2.31.0

# Motor PULL (Protrack y cualquier proveedor configurado via UI)
jsonpath_ng>=1.6.0,<2.0.0

# Dependencias opcionales (ej. importadas dinámicamente en health check y redis queue)
# redis>=5.0.0
colorlog>=6.10.1
python-json-logger>=2.0.0

# Testing
pytest>=7.4.0
pytest-asyncio>=0.21.0
